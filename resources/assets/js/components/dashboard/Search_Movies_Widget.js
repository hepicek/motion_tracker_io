import React, { Component } from 'react';
import SEARCH_MOVIES_WIDGET from "./Top_Movies_Widget";


const SEARCH_MOVIES_WIDGET = (props) => {
        let searchResults = props.searchResults.map(movie => (
            <div>
                
            </div>

            ));
    };





export default SEARCH_MOVIES_WIDGET;